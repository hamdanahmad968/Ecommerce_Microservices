spring:
  application:
    name: configserver

  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest

  profiles:
    active: native

  cloud:
    config:
      server:
        native:
          search-locations: file:///C:\Users\mdham\Downloads\Eccomerce_microservices\configserver\src\main\resources\config

management:
  endpoints:
    web:
      exposure:
        include: "*"

server:
  port: 8888